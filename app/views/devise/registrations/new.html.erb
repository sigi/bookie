<h2><%= t('.sign_up') %></h2>

<p>
<ul style="text-align: left">
  <li>
    Dein Realname wird anderen Spielern angezeigt und erscheint so in der
    Rangliste.
  </li>
  <li>
    Deine E-Mail wird anderen Spielern <b>nicht</b> angezeigt. Die E-Mail ist
    erforderlich zum Anmelden. Wir schicken dorthin auch Informationen, das
    Tippspiel betreffend.
  </li>
</ul>
</p>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :real_name %><br />
    <%= f.text_field :real_name, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit t('.sign_up') %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
